{
  "name": "@hospital-tasks/database",
  "version": "1.0.0",
  "description": "Database schema and migrations",
  "type": "module",
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "studio": "drizzle-kit studio",
    "seed": "bun run src/seed.ts",
    "reset": "bun run src/reset.ts"
  },
  "dependencies": {
    "@hospital-tasks/shared": "workspace:*",
    "drizzle-orm": "^0.33.0",
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.24.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
